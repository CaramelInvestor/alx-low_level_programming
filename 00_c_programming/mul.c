#include <stdio.h>

main()
{
	int x, y, c;

	x = 5;
	y = 6;
	c = (x * y);
	printf("Multiplication of %d and %d is %d\n", x, y, (x * y));
	return (c);
	int *p = &x;

	printf("Value of Multiplication of x and y: %d\n", c);
	return (0);
}
