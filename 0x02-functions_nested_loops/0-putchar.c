#include <stdio.h>

/**
 * main -Serves as an entry point to the program
 *
 * Return: Return zero if no error is encountered,
 * else return a non-zero number
 */

int main(void)
{
	{
		write("_putchar\n");
	}
	return(0)
}
