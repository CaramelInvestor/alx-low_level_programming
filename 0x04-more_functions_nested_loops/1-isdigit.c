#include "main.h"

/**
 * main - _isdigit checks for a digit (0 through 9).
 *
 * Return: 1 if c is a digit 0 otherwise
 */

int _isdigit(int c);
{
	int i;
	if (i == 0; i <= 9;)
	{
		_putchar(i);
		i++
		return (1);
	}
	else
		return (0);
}
